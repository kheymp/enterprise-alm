services:
  postgres:
    image: postgres:16
    container_name: alm-postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: enterprise_alm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
