import { Container, Typography } from "@mui/material";

export default function Welcome() {
    return (
        <Container>
            <Typography>
                Welcome to Enterprise ALM. Dashboards and Asset tracking are coming in M2.
            </Typography>
        </Container>
    );
}